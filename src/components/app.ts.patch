@@ -339,9 +339,21 @@
     if (this.displayedGists.length === 0) {
       const hasGists = gistStore.getGists().length > 0;
       if (!hasGists && !gistStore.getIsLoading())
-        return '<div class="empty-state"><p>No gists yet. Create your first gist!</p></div>';
+        return EmptyState.render({
+          title: 'No Gists Yet',
+          description: 'Ready to share some code? Create your first GitHub Gist to get started.',
+          actionLabel: 'Create Gist',
+          actionRoute: 'create',
+          icon: '📝'
+        });
       if (this.searchQuery)
-        return '<div class="empty-state"><p>No gists match your search.</p></div>';
-      return '<div class="empty-state"><p>No gists found.</p></div>';
+        return EmptyState.render({
+          title: 'No Matches',
+          description: `We couldn't find any gists matching "${this.searchQuery}".`,
+          actionLabel: 'Clear Search',
+          onAction: () => { this.searchQuery = ''; this.updateGistList(); },
+          icon: '🔍'
+        });
+      return '<div class="empty-state-container"><p>No gists found.</p></div>';
     }
     return this.displayedGists.map(renderCard).join('');
   }
