# pnpm configuration
# Prevents interactive prompts in CI/non-TTY environments
confirm-modules-purge=false

# Only allow specified packages to run install scripts (pnpm v11+)
onlyBuiltDependencies[]=canvas
onlyBuiltDependencies[]=esbuild
