# Plan: Fix All GitHub Actions Failures

## Current State

### Open Issues (1)
| # | Title | Type |
|---|-------|------|
| 213 | F-Droid: Submit MR to fdroiddata | enhancement |

No CI involvement.

### Open PRs (2)

#### PR #249 — "⚡ Bolt: consolidate home list processing"
- Branch: `bolt-home-perf-14402008132567069441`
- Files: `src/routes/home.ts`
- **All 20+ GitHub Actions pass.** Only Codacy (third-party) fails — not a GitHub Action.

#### PR #248 — "🛡️ Sentinel: Fix Obfuscated Protocol Bypass in sanitizeUrl"
- Branch: `fix/sanitize-url-obfuscation-bypass-18173282418529031174`
- Files: 11 files (security-dom.ts, 3 test files, components, routes)
- **Failures:**
  1. **Validate Commits** — commit body line exceeds 250-char limit (`body-max-line-length` rule)
  2. **Quality Gate** — likely cascading typecheck failure (generated tokens missing)
  3. **Codacy** — third-party, action_required

### Main Branch — Mutation Testing (scheduled)
- **Failure**: Stryker TypeScript checker can't find `src/tokens/generated/tokens.ts`
- **Root cause**: `mutation-testing.yml` does NOT run `pnpm run tokens:build` before `stryker run`. The file is `.gitignored` so CI checkouts never have it. The CI workflow's `quality_gate.sh` and `quick-tests` job both run `tokens:build` first, but `mutation-testing.yml` skips it.

---

## Fixes

### Fix 1: Mutation Testing — Add token build step
**File:** `.github/workflows/mutation-testing.yml`

Add `pnpm run tokens:build` before the Stryker step (line 28). This mirrors what `ci.yml`'s `quality_gate.sh` (line 14) and `quick-tests` (line 240) already do.

```yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Build design tokens          # <-- ADD THIS
        run: pnpm run tokens:build         # <-- ADD THIS
      - name: Run Stryker Mutation Tests
        run: pnpm exec stryker run stryker.config.json
```

### Fix 2: PR #248 — Fix commit message line length
**Action:** Amending the commit or force-pushing with a corrected message where all body lines are ≤ 250 characters. The failing line is in the commit body. Options:
- Interactive rebase to fix the commit message
- Or squash-merge with a corrected message

**Note:** This is on a Jules-created branch. The commit message body has a line > 250 chars.

---

## What NOT to fix
- **Codacy failures** on PR #248 and #249 — Codacy is a third-party static analysis service, not a GitHub Action. It requires manual intervention on the Codacy dashboard (`action_required`), or may need configuration changes outside the scope of GH Actions.
- **PR #249** — All GitHub Actions already pass. No action needed.
- **Issue #213** — Enhancement request, no CI involvement.

---

## Verification

1. After editing `mutation-testing.yml`, validate the YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/mutation-testing.yml'))"`
2. Run locally to confirm tokens build: `pnpm run tokens:build`
3. Verify the stryker config still works: `pnpm exec stryker run stryker.config.json --dry-run` (or similar)
4. Push changes and confirm Mutation Testing workflow passes on next scheduled run or via `workflow_dispatch`

---

## Summary of changes
| File | Change |
|------|--------|
| `.github/workflows/mutation-testing.yml` | Add `pnpm run tokens:build` step before Stryker |
| PR #248 commits | Fix commit body line length (≤ 250 chars) |
